/** Workbox runtime cache limits (plain JS — imported by `src/sw.js` service worker build). */
export const WORKBOX_ASSETS_MAX_ENTRIES = 100;
export const WORKBOX_ASSETS_MAX_AGE_SECONDS = 60 * 60 * 24 * 30;
