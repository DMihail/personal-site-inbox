/** Shared flex child that scrolls vertically (use inside `flex flex-col min-h-0` parents). */
export const scrollPaneClass =
  "flex-1 min-h-0 overflow-y-auto overscroll-y-contain [-webkit-overflow-scrolling:touch]";
