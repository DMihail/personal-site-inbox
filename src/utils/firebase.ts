/** @deprecated Prefer `firebaseAuth` or `getFirestoreDb` for tree-shaking. */
export { firebaseApp } from "./firebaseApp";
export { firebaseAuth } from "./firebaseAuth";
export { getFirestoreDb } from "./firestore";
